<%= form_for(@directory, :html => {:multipart => true}) do |f| %>
  <% if @directory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@directory.errors.count, "error") %> prohibited this directory from being saved:</h2>

      <ul>
      <% @directory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
  <%= f.label :title %>
  <%= f.text_field :title %>
  </p>

  <%= has_media_field @directory, :avatar %>

  <%= has_media_field @directory, :images %>

  <%= has_media_field @directory, :audios %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
